# 查询构建器（概览）

本章是查询构建器的导航页，完整的使用说明已经整理在：[查询构建器](query.md)。

如果你刚接触 Goal-Web，可以按下面的顺序阅读：

1. [数据库入门](getting-started.md)
2. [查询构建器](query.md)
3. [模型和 ORM](orm.md)

在 `query.md` 中，你将看到：

- 如何获取 `contracts.DBConnection`
- 如何执行原生 SQL（`Select`、`Statement` 等）
- 使用 `Table`、`Where`、`OrderBy`、`Limit` 等构建复杂查询
- 聚合、分页、连接、子查询等高级用法

本文件仅作为目录结构中的占位和跳转入口，避免用户误认为查询相关文档缺失。

